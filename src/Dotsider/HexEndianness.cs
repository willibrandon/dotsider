namespace Dotsider;

/// <summary>Endianness for the hex data interpretation panel.</summary>
public enum HexEndianness
{
    /// <summary>Little-endian byte order (least significant byte first).</summary>
    Little,

    /// <summary>Big-endian byte order (most significant byte first).</summary>
    Big
}
