---
title: dotsider
description: A TUI for analyzing .NET assemblies — structure, metadata, IL, strings, dependencies, and more.
template: splash
head:
  - tag: title
    content: dotsider - Analyze .NET assemblies like a boss
hero:
  tagline: A TUI for analyzing .NET assemblies
  actions:
    - text: Get Started
      link: /getting-started/installation/
      icon: right-arrow
    - text: Try It Live
      link: /demo/
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<div class="hero-prompt">
  <span class="prompt-char">$</span> dotsider MyApp.dll
</div>

<div class="feature-strip">
  <div class="feature-item">
    <span class="feature-key">1-8</span>
    <span class="feature-label">8 analysis tabs — general, PE/metadata, IL, strings, hex, dep graph, size map, runtime tracing</span>
  </div>
  <div class="feature-item">
    <span class="feature-key">diff</span>
    <span class="feature-label">Side-by-side assembly comparison. See what changed between two versions.</span>
  </div>
  <div class="feature-item">
    <span class="feature-key">.nupkg</span>
    <span class="feature-label">Open NuGet packages directly. Browse contents, drill into any DLL inside.</span>
  </div>
  <div class="feature-item">
    <span class="feature-key">cli</span>
    <span class="feature-label">Analyze, diff, inspect, and trace from the command line.</span>
  </div>
</div>

<div class="install-hint">
  <code>dotnet tool install -g dotsider</code>
  <button class="copy-btn" aria-label="Copy install command">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
    </svg>
  </button>
</div>

<script>
{`
  const btn = document.querySelector('.install-hint .copy-btn');
  btn.addEventListener('click', () => {
    navigator.clipboard.writeText('dotnet tool install -g dotsider');
    btn.classList.add('copied');
  });
  btn.addEventListener('mouseenter', () => {
    btn.classList.remove('copied');
  });
`}
</script>
