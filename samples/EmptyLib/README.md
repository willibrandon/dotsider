# EmptyLib

Minimal library with a single internal placeholder class. Used to test dotsider's handling of near-empty assemblies with sparse metadata — edge cases like zero public types, no entry point, and minimal IL.
